# AhaMark 接手规则

所有接手本仓库的任务，在检查或修改代码前，必须先完整阅读根目录 `README.md` 的“接手必读：当前状态、问题与下一步”章节，并按其中的当前基线、验证状态、遗留问题和安全边界继续工作。

`README.md` 的该章节是唯一状态账本。每次实际修改都必须同步更新该章节，记录修改内容、原因、已解决和待解决问题、验证结果及下一位接手事项；不要在本文件复制第二份状态账本。
