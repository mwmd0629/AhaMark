# 异步任务

`workers/` 保存 Celery 应用、请求上下文传播和异步任务实现：

- `celery_app.py`：Celery 配置与任务注册；
- `task_context.py`：任务请求上下文；
- `tasks/`：识别、处理、评分建议、作业生成、报表和演示重置任务。

Worker 只能生成候选、衍生数据或受契约约束的后台结果，不能绕过教师确认直接发布成绩。新增任务时应同时补充 API/模型契约测试、幂等或重试行为测试以及对应文档；运行时输出不得写入源码目录。
